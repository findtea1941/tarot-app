---
description: 牌阵录入与牌阵回顾区域为固定布局，修改前需与用户确认
globs: app/tarot/**/spread/**/*.tsx, app/tarot/**/result/page.tsx, components/*ReviewBoard.tsx, components/*EntryBoard.tsx
alwaysApply: false
---

# 牌阵录入与牌阵回顾区域固定约定

- **牌阵录入界面**（`app/tarot/[caseId]/spread/page.tsx` 中「牌阵录入」区块及其调用的各 `*EntryBoard` 组件）与 **牌阵回顾界面**（`app/tarot/[caseId]/result/page.tsx` 中「牌阵回顾」区块及其调用的各 `*ReviewBoard` 组件）的 **视觉大小与位置已按当前设计固定**。
- 后续若需调整这两部分的大小、位置或布局，**必须先与用户文字确认**，不得自行改动。
- 其它页面区块（如案例基本信息、统筹分析表格、数字计算等）的调整不受此限制，但不得影响牌阵录入/牌阵回顾的既有尺寸与位置。
